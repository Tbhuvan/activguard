# ActivGuard — Python ≥3.11 required
# Core ML / activation probe
torch>=2.0.0
transformers>=4.35.0

# Semantic RAG — Layer 2
chromadb>=0.4.0

# HTTP clients for connectors
requests>=2.31.0
httpx>=0.25.0

# Numerical computing
numpy>=1.24.0
scipy>=1.11.0

# Data model / serialisation
dataclasses-json>=0.6.0
pydantic>=2.0.0

# CLI interface
click>=8.1.0
rich>=13.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0

# Code quality
mypy>=1.5.0
ruff>=0.1.0
black>=23.0.0

# Optional: formal verification (requires separate Nagini installation)
# nagini  — install separately: pip install nagini

# Security scanning
bandit>=1.7.0
